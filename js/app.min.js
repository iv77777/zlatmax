/*
 * ATTENTION: The "eval" devtool has been used (maybe by default in mode: "development").
 * This devtool is neither made for production nor for readable output files.
 * It uses "eval()" calls to create a separate source file in the browser devtools.
 * If you are trying to read the output file, select a different devtool (https://webpack.js.org/configuration/devtool/)
 * or disable the default devtool with "devtool: false".
 * If you are looking for production-ready output files, see mode: "production" (https://webpack.js.org/configuration/mode/).
 */
/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./src/js/app.js":
/*!***********************!*\
  !*** ./src/js/app.js ***!
  \***********************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _libs_dynamic_adapt_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./libs/dynamic_adapt.js */ \"./src/js/libs/dynamic_adapt.js\");\n/* harmony import */ var _modules_functions_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./modules/functions.js */ \"./src/js/modules/functions.js\");\n/* harmony import */ var _files_script_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./files/script.js */ \"./src/js/files/script.js\");\n\r\n// =============================================================================\r\n/* Динамический адаптив */ \r\n\r\n// =============================================================================\r\n// импортируем все функции из ./function.js\r\n\r\n\r\n// проверка поддержки webp, добавления класса webp или no-webp для тега HTML\r\n// myFunction.isWebp();\r\n\r\n// Модуль для роботы с меню (Бургер)\r\n_modules_functions_js__WEBPACK_IMPORTED_MODULE_1__.initMenu();\r\n\r\n// Модуль для роботы со спойлерами\r\n// Сниппет (HTML): spollers\r\n_modules_functions_js__WEBPACK_IMPORTED_MODULE_1__.initSpollers();\r\n\r\n\r\n// Модуль для роботы c попапамы\r\n_modules_functions_js__WEBPACK_IMPORTED_MODULE_1__.initPopups(); \r\n// ==============================================================================\r\n\r\n// подключаем слайдер свайпер\r\n// import Swiper, { Navigation, Pagination } from 'swiper';\r\n// const swiper = new Swiper();\r\n\r\n// ==============================================================================\r\n\r\n// Прочее ==============================================================================\r\n/* Подключаем файлы со своим кодом */ \r\n\r\n// ==============================================================================\r\n\n\n//# sourceURL=webpack://gulpstart/./src/js/app.js?");

/***/ }),

/***/ "./src/js/files/script.js":
/*!********************************!*\
  !*** ./src/js/files/script.js ***!
  \********************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\ndocument.addEventListener(\"click\", documentActions);\r\n\r\nconst subMenuCatalogBlockAll = document.querySelectorAll('.sub-menu-catalog__block');\r\nsubMenuCatalogBlockAll.forEach(subMenuCatalogBlock =>{\r\n  const subMenuCatalogBlockLength = subMenuCatalogBlock.querySelectorAll('.sub-menu-catalog__category').length;\r\n  subMenuCatalogBlock.classList.add(`sub-menu-catalog__block_${subMenuCatalogBlockLength}`);\r\n});\r\n\r\nfunction documentActions(e){\r\n  const targetElement = e.target;\r\n  if (targetElement.closest('[data-parent]')){\r\n    const subMenuItem = targetElement.closest('[data-parent]');\r\n    const subMenuId = subMenuItem.dataset.parent;\r\n    if (subMenuId){\r\n      const subMenuCatalog = document.querySelector(`[data-submenu='${subMenuId}']`);\r\n      if (subMenuCatalog) {\r\n        const subMenuCatalogActive = document.querySelector('._sub-menu-open');\r\n        if (subMenuCatalogActive && subMenuCatalogActive !== subMenuCatalog){\r\n          subMenuCatalogActive.classList.remove('_sub-menu-open');\r\n          const subMenuItemActive = document.querySelector('._sub-menu-active');\r\n          subMenuItemActive.classList.remove('_sub-menu-active');\r\n        }\r\n        subMenuItem.classList.toggle('_sub-menu-active');\r\n        subMenuCatalog.classList.toggle('_sub-menu-open');\r\n      }\r\n    }\r\n  }\r\n\r\n  if (targetElement.closest('.menu-top-header__item_catalog')){\r\n    document.documentElement.classList.add('_catalog-open');\r\n  }\r\n  if (targetElement.closest('.menu-catalog__back')){\r\n    document.documentElement.classList.remove('_catalog-open');\r\n  }\r\n  if (targetElement.closest('.sub-menu-catalog__back')){\r\n    document.querySelector('._sub-menu-open').classList.remove('_sub-menu-open');\r\n  }\r\n}\n\n//# sourceURL=webpack://gulpstart/./src/js/files/script.js?");

/***/ }),

/***/ "./src/js/libs/dynamic_adapt.js":
/*!**************************************!*\
  !*** ./src/js/libs/dynamic_adapt.js ***!
  \**************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\nfunction DynamicAdapt(type) {\r\n  this.type = type;\r\n}\r\n\r\nDynamicAdapt.prototype.init = function () {\r\n  const _this = this;\r\n  // массив объектов\r\n  this.оbjects = [];\r\n  this.daClassname = \"_dynamic_adapt_\";\r\n  // массив DOM-элементов\r\n  this.nodes = document.querySelectorAll(\"[data-da]\");\r\n\r\n  // наполнение оbjects объктами\r\n  for (let i = 0; i < this.nodes.length; i++) {\r\n    const node = this.nodes[i];\r\n    const data = node.dataset.da.trim();\r\n    const dataArray = data.split(\",\");\r\n    const оbject = {};\r\n    оbject.element = node;\r\n    оbject.parent = node.parentNode;\r\n    оbject.destination = document.querySelector(dataArray[0].trim());\r\n    оbject.breakpoint = dataArray[1] ? dataArray[1].trim() : \"767\";\r\n    оbject.place = dataArray[2] ? dataArray[2].trim() : \"last\";\r\n    оbject.index = this.indexInParent(оbject.parent, оbject.element);\r\n    this.оbjects.push(оbject);\r\n  }\r\n\r\n  this.arraySort(this.оbjects);\r\n\r\n  // массив уникальных медиа-запросов\r\n  this.mediaQueries = Array.prototype.map.call(this.оbjects, function (item) {\r\n    return '(' + this.type + \"-width: \" + item.breakpoint + \"px),\" + item.breakpoint;\r\n  }, this);\r\n  this.mediaQueries = Array.prototype.filter.call(this.mediaQueries, function (item, index, self) {\r\n    return Array.prototype.indexOf.call(self, item) === index;\r\n  });\r\n\r\n  // навешивание слушателя на медиа-запрос\r\n  // и вызов обработчика при первом запуске\r\n  for (let i = 0; i < this.mediaQueries.length; i++) {\r\n    const media = this.mediaQueries[i];\r\n    const mediaSplit = String.prototype.split.call(media, ',');\r\n    const matchMedia = window.matchMedia(mediaSplit[0]);\r\n    const mediaBreakpoint = mediaSplit[1];\r\n\r\n    // массив объектов с подходящим брейкпоинтом\r\n    const оbjectsFilter = Array.prototype.filter.call(this.оbjects, function (item) {\r\n      return item.breakpoint === mediaBreakpoint;\r\n    });\r\n    matchMedia.addListener(function () {\r\n      _this.mediaHandler(matchMedia, оbjectsFilter);\r\n    });\r\n    this.mediaHandler(matchMedia, оbjectsFilter);\r\n  }\r\n};\r\n\r\nDynamicAdapt.prototype.mediaHandler = function (matchMedia, оbjects) {\r\n  if (matchMedia.matches) {\r\n    for (let i = 0; i < оbjects.length; i++) {\r\n      const оbject = оbjects[i];\r\n      оbject.index = this.indexInParent(оbject.parent, оbject.element);\r\n      this.moveTo(оbject.place, оbject.element, оbject.destination);\r\n    }\r\n  } else {\r\n    for (let i = 0; i < оbjects.length; i++) {\r\n      const оbject = оbjects[i];\r\n      if (оbject.element.classList.contains(this.daClassname)) {\r\n        this.moveBack(оbject.parent, оbject.element, оbject.index);\r\n      }\r\n    }\r\n  }\r\n};\r\n\r\n// Функция перемещения\r\nDynamicAdapt.prototype.moveTo = function (place, element, destination) {\r\n  element.classList.add(this.daClassname);\r\n  if (place === 'last' || place >= destination.children.length) {\r\n    destination.insertAdjacentElement('beforeend', element);\r\n    return;\r\n  }\r\n  if (place === 'first') {\r\n    destination.insertAdjacentElement('afterbegin', element);\r\n    return;\r\n  }\r\n  destination.children[place].insertAdjacentElement('beforebegin', element);\r\n}\r\n\r\n// Функция возврата\r\nDynamicAdapt.prototype.moveBack = function (parent, element, index) {\r\n  element.classList.remove(this.daClassname);\r\n  if (parent.children[index] !== undefined) {\r\n    parent.children[index].insertAdjacentElement('beforebegin', element);\r\n  } else {\r\n    parent.insertAdjacentElement('beforeend', element);\r\n  }\r\n}\r\n\r\n// Функция получения индекса внутри родителя\r\nDynamicAdapt.prototype.indexInParent = function (parent, element) {\r\n  const array = Array.prototype.slice.call(parent.children);\r\n  return Array.prototype.indexOf.call(array, element);\r\n};\r\n\r\n// Функция сортировки массива по breakpoint и place \r\n// по возрастанию для this.type = min\r\n// по убыванию для this.type = max\r\nDynamicAdapt.prototype.arraySort = function (arr) {\r\n  if (this.type === \"min\") {\r\n    Array.prototype.sort.call(arr, function (a, b) {\r\n      if (a.breakpoint === b.breakpoint) {\r\n        if (a.place === b.place) {\r\n          return 0;\r\n        }\r\n\r\n        if (a.place === \"first\" || b.place === \"last\") {\r\n          return -1;\r\n        }\r\n\r\n        if (a.place === \"last\" || b.place === \"first\") {\r\n          return 1;\r\n        }\r\n\r\n        return a.place - b.place;\r\n      }\r\n\r\n      return a.breakpoint - b.breakpoint;\r\n    });\r\n  } else {\r\n    Array.prototype.sort.call(arr, function (a, b) {\r\n      if (a.breakpoint === b.breakpoint) {\r\n        if (a.place === b.place) {\r\n          return 0;\r\n        }\r\n\r\n        if (a.place === \"first\" || b.place === \"last\") {\r\n          return 1;\r\n        }\r\n\r\n        if (a.place === \"last\" || b.place === \"first\") {\r\n          return -1;\r\n        }\r\n\r\n        return b.place - a.place;\r\n      }\r\n\r\n      return b.breakpoint - a.breakpoint;\r\n    });\r\n    return;\r\n  }\r\n};\r\n\r\nconst da = new DynamicAdapt(\"max\");\r\nda.init();\n\n//# sourceURL=webpack://gulpstart/./src/js/libs/dynamic_adapt.js?");

/***/ }),

/***/ "./src/js/modules/functions.js":
/*!*************************************!*\
  !*** ./src/js/modules/functions.js ***!
  \*************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export */ __webpack_require__.d(__webpack_exports__, {\n/* harmony export */   \"initMenu\": () => (/* binding */ initMenu),\n/* harmony export */   \"initPopups\": () => (/* binding */ initPopups),\n/* harmony export */   \"initSpollers\": () => (/* binding */ initSpollers),\n/* harmony export */   \"isWebp\": () => (/* binding */ isWebp)\n/* harmony export */ });\n// проверка поддержки webp, добавления класса webp или no-webp для тега HTML\r\nfunction isWebp(){\r\n  // проверка поддержки webp\r\n  function testWebP(callback) {\r\n    let webP = new Image();\r\n    webP.onload = webP.onerror = function () {\r\n      callback(webP.height == 2);\r\n    };\r\n    webP.src = \"data:image/webp;base64,UklGRjoAAABXRUJQVlA4IC4AAACyAgCdASoCAAIALmk0mk0iIiIiIgBoSygABc6WWgAA/veff/0PP8bA//LwYAAA\";\r\n  }\r\n\r\n  // Добавление класса webp или no-webp для HTML\r\n  testWebP(function (support) {\r\n    if (support == true) {\r\n      document.querySelector('body').classList.add('webp');\r\n    } else {\r\n      document.querySelector('body').classList.add('no-webp');\r\n    }\r\n  });\r\n}\r\n// ===============================================================================\r\n\r\n// -------------------------------------------------------------------------------\r\n\r\n// ===============================================================================\r\n// Открывает закрывает меню (Бургер)\r\nfunction initMenu(){\r\n  const iconMenu = document.querySelector('.icon-menu_js');\r\n  if(iconMenu){\r\n    iconMenu.addEventListener('click', () =>{\r\n      document.documentElement.classList.toggle('_menu-open');\r\n      if(document.documentElement.classList.contains('_catalog-open')){\r\n        document.documentElement.classList.remove('_catalog-open');\r\n\r\n        const subMenuOpen = document.querySelector('._sub-menu-open');\r\n        if(subMenuOpen){\r\n          subMenuOpen.classList.remove('_sub-menu-open');\r\n        }\r\n      }\r\n      // добавить блокировку страницы bodyLockToggle();\r\n    });\r\n  }\r\n}   \r\n// ===============================================================================\r\n\r\n// -------------------------------------------------------------------------------\r\n\r\n// ===============================================================================\r\n// Для родитиля спойлеров пишем атрибут data-spollers\r\n// Для заголовка спойлера пишем фтрибут data-spoller\r\n\r\n// Если нужно что бы в блоке открывался только один спойлер добавляем атрибут\r\n// data-one-spoller Для родитиля\r\n\r\n// Если нужно включить/выключить работу спойлеров на разных размерах экранов\r\n// пишем параметры ширины и типа брейкпоинта. Для обгортки конкретного сполера\r\n// data-spollers=\"800,max\" - спойлеры будут работать только на экранах менше или равно 800px\r\n// data-spollers=\"650,min\" - спойлеры будут работать только на экранах больше или равно 650px\r\n\r\n// Модуль для роботы со спойлерами\r\nfunction initSpollers(){\r\n  // в переменною \"spollersArray\" получаем колэкцию из всех обектов в которых есть дата атрибут [data-spollers]\r\n  const spollersArray = document.querySelectorAll('[data-spollers]');\r\n\r\n  // если \"spollersArray\" не пустой \r\n  if (spollersArray.length > 0) {\r\n    //В переменною \"spollersRegular\" Получаем все спойлера в которых нету медиа запроссов (console.log(spollersRegular);)\r\n    const spollersRegular = Array.from(spollersArray).filter(function (item, index, self) {\r\n      return !item.dataset.spollers.split(\",\")[0];\r\n    });\r\n    // Если есть такие спойлера то запускаем функцию initSpollers(spollersRegular); в которою передаем масив с етими спойлерами\r\n    if (spollersRegular.length > 0) {\r\n      // запускаем функцию initSpollers (Иницилизирует ети спойлери)\r\n      initSpollers(spollersRegular);\r\n    }\r\n\r\n    //В переменною \"spollersMedia\" Получаем все спойлера в которых есть медиа запроссы ( console.log(spollersMedia); = [div.block.block_2, div.block.block_3] итд.)\r\n    const spollersMedia = Array.from(spollersArray).filter(function (item, index, self) {\r\n      return item.dataset.spollers.split(\",\")[0];\r\n    });\r\n\r\n    // Если есть такие спойлера (c медиа запросами) то\r\n    if (spollersMedia.length > 0) {\r\n      // в массиве \"breakpointsArray\" будут созданные обекты для каждого спойлера в которых есть медиа запроссы  [{value: \"650\", type: \"min\", item: div.block.block_2}, {value: \"800\", type: \"max\", item: div.block.block_3} итд.]\r\n      const breakpointsArray = [];\r\n      // у каждого етого спойлера [div.block.block_2, div.block.block_3]\r\n      spollersMedia.forEach(item => {\r\n        //из html < data-spollers=\"650,min\"> получаем строку с параметрами < 650,min >\r\n        const params = item.dataset.spollers;\r\n        //в обект \"breakpoint\" будем ложить брекпоинт, значения и сам елемент (спойлер) {value: \"650\", type: \"min\", item: div.block.block_2} \r\n        const breakpoint = {};\r\n        // раздиляем строку параметра на масив  [\"650\", \"min\"]\r\n        const paramsArrey = params.split(\",\");\r\n        // из масива \"paramsArrey\" ложим значения по индексу [0] (в данном случаи \"650\") в обект \"breakpoint\" <value: \"650\">\r\n        breakpoint.value = paramsArrey[0];\r\n        // из масива \"paramsArrey\" ложим значения по индексу [1] (в данном случаи \"min\") в обект \"breakpoint\" < type: \"min\"> если не указан то поумолчанию будт < type: \"max\">\r\n        breakpoint.type = paramsArrey[1] ? paramsArrey[1].trim() : \"max\";\r\n        // в обект \"breakpoint\" добавляем етот елемент (спойлер) < item: div.block.block_2 >\r\n        breakpoint.item = item;\r\n        // в массив \"breakpointsArray\" додаем созданний обект \"breakpoint\" для етот спойлера\r\n        breakpointsArray.push(breakpoint);\r\n\r\n      });\r\n\r\n      // формируем строки медиа запроссов [\"{min-width:650px),650,min\", \"{min-width:650px),650,min\", \"{max-width:800px),800,max\"]\r\n      let mediaQueries = breakpointsArray.map(function (item) {\r\n        return '(' + item.type + \"-width:\" + item.value + \"px),\" + item.value + ',' + item.type;\r\n      });\r\n      // убераем повторы медиа запроссов [\"{min-width:650px),650,min\", \"{max-width:800px),800,max\"]\r\n      mediaQueries = mediaQueries.filter(function (item, index, self) {\r\n        // \r\n        return self.indexOf(item) === index;\r\n      });\r\n\r\n      // робота з кажним брейкпоинтом\r\n      mediaQueries.forEach(breakpoint => {\r\n        // розбиваем на [\"(min-width:650px)\", \"650\", \"min\"] итд.\r\n        const paramsArrey = breakpoint.split(\",\");\r\n        // кладьом 650\r\n        const mediaBreakpoint = paramsArrey[1];\r\n        // кладьом min\r\n        const mediaType = paramsArrey[2];\r\n        // бедет слушать ширину екрана например (min-width:650px) \r\n        const matchMedia = window.matchMedia(paramsArrey[0]);\r\n\r\n        // сортируем обекти с нужным условиям (одинаковые вмести)\r\n        const spollersArray = breakpointsArray.filter(function (item) {\r\n          if (item.value === mediaBreakpoint && item.type === mediaType) {\r\n            return true;\r\n          }\r\n        });\r\n\r\n        // При сробатовании matchMedia запускаем функию initSpollers(spollersArray, matchMedia); в которою передаем параметры\r\n        matchMedia.addListener(function () {\r\n          initSpollers(spollersArray, matchMedia);\r\n        });\r\n        // запускаем функцию initSpollers сразу при загрузке страницы\r\n        initSpollers(spollersArray, matchMedia);\r\n\r\n      });\r\n\r\n    }\r\n\r\n    // Иницилизация\r\n    function initSpollers(spollersArray, matchMedia = false) {\r\n\r\n      spollersArray.forEach(spollersBlock => {\r\n        // если matchMedia дает true тогда присваюваем spollersBlock.item а если  false spollersBlock;\r\n        spollersBlock = matchMedia ? spollersBlock.item : spollersBlock;\r\n\r\n        // если сработал брейкпоинт или он не бил задан спойлкру тогда\r\n        if (matchMedia.matches || !matchMedia) {\r\n          // оболочке спойлера присваеватсь класс '_init'\r\n          spollersBlock.classList.add('_init');\r\n          // запускаем функцию initSpollerBody\r\n          initSpollerBody(spollersBlock);\r\n          // при клики на spollersBlock вызываем функцию\r\n          spollersBlock.addEventListener(\"click\", setSpollerAction);\r\n        } else {\r\n          // отбераем у spollersBlock класс '_init'\r\n          spollersBlock.classList.remove('_init');\r\n          // запускаем функцию initSpollerBody\r\n          initSpollerBody(spollersBlock, false);\r\n          // убераем события клик \r\n          spollersBlock.removeEventListener(\"click\", setSpollerAction);\r\n        }\r\n\r\n      });\r\n    }\r\n    // Робота с контентом\r\n    function initSpollerBody(spollersBlock, hideSpollerBody = true) {\r\n      // получаем все заголовки спойлеров внутри блока spollersBlock\r\n      const spollerTitles = spollersBlock.querySelectorAll('[data-spoller]');\r\n      // если есть заголовки \r\n      if (spollerTitles.length > 0) {\r\n        // то для каждого заголовка\r\n        spollerTitles.forEach(spollerTitle => {\r\n          // если под етим заголовком сойлер скрыт то\r\n          if (hideSpollerBody) {\r\n            // у етого заголовка убераем атрибут таб-индекс (ето включает возможность переходить по етим заголовкам нажатиям на клавишу таб)\r\n            spollerTitle.removeAttribute('tabindex');\r\n            // если у етого заголовка нету класса '_active'\r\n            if (!spollerTitle.classList.contains('_active')) {\r\n              // тогда скриваем контентною часть\r\n              spollerTitle.nextElementSibling.hidden = true;\r\n            }\r\n          } else {\r\n            // если под данным заголовком спойлер открыт то\r\n            // добавляем таб индекс -1 \r\n            spollerTitle.setAttribute('tabindex', '-1');\r\n            // разкрываем спойлеры если они били скрыти\r\n            spollerTitle.nextElementSibling.hidden = false;\r\n          }\r\n        });\r\n      }\r\n    }\r\n    // Выполняется когда кликаем на заголовок спойлера\r\n    function setSpollerAction(e) {\r\n      // в \"el\" получаем нажатый обект\r\n      const el = e.target;\r\n      // если у етого обекта или у его родитиля есть 'data-spoller' то\r\n      if (el.hasAttribute('data-spoller') || el.closest('[data-spoller]')) {\r\n        // получаем сам заголовок (кнопку) спойлура\r\n        const spollerTitle = el.hasAttribute('data-spoller') ? el : el.closest('[data-spoller]');\r\n        // Находим ближайшого родитиля з атрибутом [data-spollers]\r\n        const spollersBlock = spollerTitle.closest('[data-spollers]');\r\n        // если у етого родитиля есть атрибут 'data-one-spoller' то oneSpoller будет = true, если нет то false\r\n        const oneSpoller = spollersBlock.hasAttribute('data-one-spoller') ? true : false;\r\n        // если у данного родитиля нету обекта з классом _slide то\r\n        if (!spollersBlock.querySelectorAll('_slide').length) {\r\n          // если у родитиля есть дата-атрибут 'data-one-spoller' и в заголовке спойлера нету класса '_active' то\r\n          if (oneSpoller && !spollerTitle.classList.contains('_active')) {\r\n            // запускаем функцию hideSpollerBody которая скривает все спойлеры\r\n            hideSpollerBody(spollersBlock);\r\n          }\r\n          // будет скривать или показувать слайды\r\n          _slideToggle(spollerTitle.nextElementSibling, 300);\r\n        }\r\n        // отминяет события по умолчанию\r\n        e.preventDefault();\r\n      }\r\n    }\r\n    // скривает все спойлеры\r\n    function hideSpollerBody(spollersBlock) {\r\n      // получаю открытый спойлер внутри родитильського обекта (spollersBlock)\r\n      const spollerActiveTitle = spollersBlock.querySelector('[data-spoller]._active');\r\n      // если есть такой обект то\r\n      if (spollerActiveTitle) {\r\n        // скрываем все елементы\r\n        _slideUp(spollerActiveTitle.nextElementSibling, 300);\r\n      }\r\n    }\r\n  }\r\n\r\n  // функция которая скривает анимировано елемент\r\n  let _slideUp = (target, duration = 500) => {\r\n    if (!target.classList.contains('_slide')) {\r\n      target.classList.add('_slide');\r\n      target.previousElementSibling.classList.remove('_active');\r\n      target.style.transitionProperty = 'height, margin, padding';\r\n      target.style.transitionDuration = duration + 'ms';\r\n      target.style.height = target.offsetHeight + 'px';\r\n      target.offsetHeight;\r\n      target.style.overflow = 'hidden';\r\n      target.style.height = 0;\r\n      target.style.paddingTop = 0;\r\n      target.style.paddingBottom = 0;\r\n      target.style.marginTop = 0;\r\n      target.style.marginBottom = 0;\r\n      window.setTimeout(() => {\r\n        target.hidden = true;\r\n        target.style.removeProperty('height');\r\n        target.style.removeProperty('padding-top');\r\n        target.style.removeProperty('padding-bottom');\r\n        target.style.removeProperty('margin-top');\r\n        target.style.removeProperty('margin-bottom');\r\n        target.style.removeProperty('overflow');\r\n        target.style.removeProperty('transition-duration');\r\n        target.style.removeProperty('transition-property');\r\n        target.classList.remove('_slide');\r\n      }, duration);\r\n    }\r\n  }\r\n  // функция которая анимировано показует елемент\r\n  let _slideDown = (target, duration = 500) => {\r\n    if (!target.classList.contains('_slide')) {\r\n      target.classList.add('_slide');\r\n      if (target.hidden) {\r\n        target.hidden = false;\r\n      }\r\n      target.previousElementSibling.classList.add('_active');\r\n      let height = target.offsetHeight;\r\n      target.style.overflow = 'hidden';\r\n      target.style.height = 0;\r\n      target.style.paddingTop = 0;\r\n      target.style.paddingBottom = 0;\r\n      target.style.marginTop = 0;\r\n      target.style.marginBottom = 0;\r\n      target.offsetHeight;\r\n      target.style.transitionProperty = 'height, margin, padding';//+\r\n      target.style.transitionDuration = duration + 'ms';//+\r\n      target.style.height = height + 'px';\r\n      target.style.removeProperty('padding-top');\r\n      target.style.removeProperty('padding-bottom');\r\n      target.style.removeProperty('margin-top');\r\n      target.style.removeProperty('margin-bottom');\r\n      window.setTimeout(() => {\r\n        target.style.removeProperty('height');\r\n        target.style.removeProperty('overflow');\r\n        target.style.removeProperty('transition-duration');\r\n        target.style.removeProperty('transition-property');\r\n        target.classList.remove('_slide');\r\n      }, duration);\r\n    }\r\n  }\r\n  // функция которая анимировано показует елемент или скрывает\r\n  let _slideToggle = (target, duration = 500) => {\r\n    if (target.hidden) {\r\n      return _slideDown(target, duration);\r\n    } else {\r\n      return _slideUp(target, duration);\r\n    }\r\n  }\r\n}\r\n// ===============================================================================\r\n\r\n// -------------------------------------------------------------------------------\r\n\r\n// ===============================================================================\r\n// 1) Чтоби запустить попап нада добавить data - popup=\"popup1, 200\" к кнопке которая будет открывать, закрывать попап(первый параметер \"popup1\" ето id попапа второй \"200\" время в \"ms\" анимацыи открытия, закрытия попапа);\r\n\r\n// <button class=\"open-popup\" data-popup=\"popup1, 200\">Откыть Попап-1 (200ms)</button>\r\n\r\n// -------------------------------------------------------------------------------\r\n\r\n// 2) Создать попап(развирнуть снипет)\r\n\r\n// <div class=\"popup\" id = \"popup1\" >\r\n//   <div class=\"popup__fake\">\r\n//     <div class=\"popup__fake-body popup__fake-body_js\"></div>\r\n//   </div>\r\n//   <div class=\"popup__content popup__content_js\">\r\n//     <button data-close type=\"button\" class=\"popup__close\">Закрыть</button>\r\n//     <div class=\"popup__body\">\r\n//       Текст попапа\r\n//     </div>\r\n//   </div>\r\n// </div> \r\n\r\n  // ---------------------------------------------------------------------------------------\r\n// 3) добавляем класс \"._lock-padding\" всем фиксированым елементам(етим елементам будим добавлять паддинг з права когда будем блокировать скрол)\r\n// 4)Подулючить стили попапа popup.scss\r\n// 5) Запустить js функцию initPopups(); в файле app.js\r\n\r\n// Открывает закрывает попап\r\nfunction initPopups() {\r\n  // Получаем все кнопки которые вызывают попап\r\n  const buttonsAllOpenPopups = document.querySelectorAll('[data-popup]');\r\n  // получаем все (Фиксированые елементы) (етим елементам будим добавлять паддинг з права когда будем блокировать скрол)\r\n  const lockPadding = document.querySelectorAll(\"._lock-padding\");\r\n  // получаем боди\r\n  const body = document.querySelector('body');\r\n\r\n  // требуется чтоби небело двойнных нажатий (чтобы коректно выщитувався паддинг)\r\n  let unlock = true;\r\n  // через сколько будем уберать паддингы\r\n  let timeout = 300;\r\n \r\n  // Если есть кнопки которые вызывают попап то\r\n  if (buttonsAllOpenPopups.length > 0) {\r\n    // Проходимся по всем кнопкам которые вызывают попап\r\n    buttonsAllOpenPopups.forEach(item => {\r\n      // и для каждой устаеавлюваем события клик\r\n      item.addEventListener('click', () => {\r\n        // При клики берем з етой кнопки значения data-popup\r\n        const valueData = item.dataset.popup;\r\n        // разбиваем в масив строк по ','\r\n        let valueDataArray = valueData.split(',')\r\n        // Получаем id попапа которого нада открыть\r\n        let idPopup = valueDataArray[0];\r\n        // если масив больше одного елемнта то\r\n        if (valueDataArray.length > 1) {\r\n          // Устанавлюем время анимации попапа\r\n          timeout = valueDataArray[1];\r\n        } else {\r\n          // Устанавлюем время анимации попапа 300ms Если небело передано в data атрибуте\r\n          timeout = 300;\r\n        }\r\n\r\n        // Открывает попап по етом id (idPopup)\r\n        openPopup(idPopup);\r\n      });\r\n    });\r\n  }\r\n\r\n  // Открывает попап\r\n  function openPopup(idPopup) {\r\n    // Ищем на странице попап з id idPopup\r\n    const popup = document.getElementById(idPopup);\r\n    if (popup && unlock) {\r\n      // добавляем падингы body и фиксированным елементам на странице, блокируем скрол\r\n      bodyLock(popup);  \r\n\r\n      const popupContent = popup.querySelector('.popup__content_js');\r\n\r\n      const popupFakeBody = popup.querySelector('.popup__fake-body_js');\r\n\r\n      let height = popupContent.offsetHeight;\r\n      let width = popupContent.offsetWidth;\r\n\r\n      // Устанавлюваем width и height фейковои подложкы\r\n      popupFakeBody.style.cssText = `\r\n        width:${width}px;\r\n        height:${height}px;\r\n        transition: all ${timeout}ms;\r\n      `;\r\n\r\n      // Запрещаем открывать, закрывать попап пока не откроется \r\n      unlock = false;\r\n      // Устанавлюваем анимацию открытия\r\n      popup.style.transition = `opacity ${timeout}ms, visibility ${timeout}ms`;\r\n      // Добавляем етому попапу класс 'popup-open' который открывает попап\r\n      popup.classList.add('_popup-open');\r\n\r\n\r\n      // Запускаем функуию которая закрывает попап и передаем ей попап который нада закрыть\r\n      popupClose(popup, popupFakeBody); \r\n    }\r\n  }\r\n\r\n  // Функця которая закрывает попап\r\n  function popupClose(popup, popupFakeBody) {\r\n    // Устанавлюваем события клик на етот попап\r\n    popup.addEventListener('click', (e) => {\r\n      // если кликнули по елементу у которого есть 'data-close' или нету у родитилях '.popup__content' то\r\n      if (e.target.hasAttribute('data-close') || !e.target.closest('.popup__content')) {\r\n        //Закрываем попап\r\n        if (unlock) {  \r\n          popup.classList.remove('_popup-open');\r\n\r\n          popupFakeBody.style.cssText = `\r\n            width: 50px;\r\n            height: 50px;\r\n            transition: all ${timeout}ms;\r\n          `;\r\n          // Скрываем контент попапа\r\n          popup.classList.remove('_popup-open-content');\r\n          // Убераем падингы, Розблокикирувуем скролл\r\n          bodyUnLock();\r\n        }\r\n      }\r\n    });\r\n  }\r\n\r\n  // добавляем падингы\r\n  function bodyLock(popup) {\r\n    // получаем ширину скрола\r\n    const lockPaddingValue = window.innerWidth - body.offsetWidth + 'px';\r\n\r\n    // если есь фиксированые или абсолютные елементы на странице то \r\n    if (lockPadding.length > 0) {\r\n      // проходимся по ним\r\n      for (let index = 0; index < lockPadding.length; index++) {\r\n        // и для каждого устанавлюваем паддинг з права у етого елемента\r\n        lockPadding[index].style.paddingRight = lockPaddingValue;\r\n      }\r\n    }\r\n\r\n    body.style.paddingRight = lockPaddingValue;\r\n    body.classList.add('_lock');\r\n\r\n    setTimeout(() => {\r\n      // Показуем контент попапа\r\n      popup.classList.add('_popup-open-content');\r\n\r\n      // Разрышаем снова открывать попап\r\n      unlock = true;\r\n    }, timeout);\r\n  }\r\n\r\n  // Убераем падингы после закрытия попапа \r\n  function bodyUnLock() {\r\n    if (lockPadding.length > 0) {\r\n      for (let index = 0; index < lockPadding.length; index++) {\r\n        const el = lockPadding[index];\r\n        el.style.paddingRight = '0';\r\n      }\r\n    }\r\n\r\n    body.style.paddingRight = '0px';\r\n    body.classList.remove('_lock');\r\n\r\n    // Разрышаем снова открывать попап\r\n    unlock = true;\r\n  }\r\n}\r\n// ===============================================================================\r\n\n\n//# sourceURL=webpack://gulpstart/./src/js/modules/functions.js?");

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
/******/ 	
/******/ 	// startup
/******/ 	// Load entry module and return exports
/******/ 	// This entry module can't be inlined because the eval devtool is used.
/******/ 	var __webpack_exports__ = __webpack_require__("./src/js/app.js");
/******/ 	
/******/ })()
;